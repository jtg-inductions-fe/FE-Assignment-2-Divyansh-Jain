import { StyledPopover } from './Popover.styles';
import { StyledPopoverProps } from './Popover.types';

/** Custom  Popover for UserProfile */
export const Popover = (props: StyledPopoverProps) => (
    <StyledPopover {...props} />
);
